{{!< default}}

{{#tag}}
<header class="tag-head" >
  <h2>{{name}}</h1>
  <h4>
    {{#if description}}
      {{description}}
    {{else}}
      A {{../pagination.total}} post collection
    {{/if}}
  </h4>
  <hr>
</header>
{{/tag}}

{{> "loop"}}
